<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../../Core/SkyAPI.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- O adaptador do navegador é compilado aqui a partir do mesmo arquivo do cliente,
         para o teste exercitar exatamente o código publicado, sem o SDK do WebAssembly. -->
    <Compile Include="../../Client/BrowserTransport.cs" />
  </ItemGroup>
</Project>
